## 1. 覆盖率基线与目标范围

- [x] 1.1 基于现有 `jacocoTestReport` 聚合结果确认全仓库的行、分支、类、方法覆盖率基线
- [x] 1.2 输出首批需要补测的目标模块与 package 热点清单，优先标记 `app`、`player_component`、`storage_component`、`anime_component`、`user_component` 及其他低覆盖高风险区域
- [x] 1.3 如现有报告不便于持续使用，补充一个复用 JaCoCo XML 的热点摘要能力，便于后续波次按相同口径追踪覆盖率变化

## 2. JVM 侧缺口补测

- [x] 2.1 为首批目标模块中的 ViewModel、协调器、Mapper、解析器、仓储、适配器与错误处理分支补充或扩展 JVM 单元测试
- [x] 2.2 为现有高风险但低覆盖的逻辑边界补充契约测试或回归测试，覆盖成功、失败与边界路径
- [x] 2.3 对当前难以测试的代码仅做最小必要的可测试性重构，抽出可注入依赖或纯逻辑边界而不改变对外行为

## 3. Android / TV 运行时回归补测

- [x] 3.1 为影响 Activity/Fragment 生命周期、播放运行时行为或系统服务交互的目标区域补充必要的仪器测试或冒烟测试
- [x] 3.2 为 TV 可达页面中涉及焦点、DPAD 导航、设置交互、播放入口/退出的关键路径补充 DPAD 驱动的回归验证
- [x] 3.3 稳定测试夹具、假数据源和运行时前置条件，避免新增的 Android/TV 测试长期脆弱或不可重复

## 4. 覆盖率验证闭环与渐进式门禁

- [x] 4.1 将覆盖率基线、热点摘要与目标波次的验证方式接入现有 Gradle / JaCoCo 流程，而不引入第二套覆盖率口径
- [x] 4.2 为首批目标模块或 package 定义基线感知的增量验证规则，避免直接启用不现实的全仓库硬阈值
- [x] 4.3 统一覆盖率改进变更的测试证据格式，要求同时给出测试任务、覆盖率结果与 `BUILD SUCCESSFUL` / `BUILD FAILED` 结论

## 5. 最终验证与结果复盘

- [x] 5.1 运行与改动范围匹配的 JVM 测试、仪器测试及 `jacocoTestReport`，确认任务链路可重复执行
- [x] 5.2 对比本波次执行前后的覆盖率结果，确认目标模块或热点区域覆盖率确有提升且未引入明显回归
- [x] 5.3 汇总本波次新增测试范围、覆盖率变化与剩余空白，作为下一轮覆盖率提升的输入
